# Secrets — ne jamais commiter
.env
.env.*
!.env.example

# Système macOS
.DS_Store
**/.DS_Store

# Éditeurs
.vscode/
.idea/
*.swp
*.swo

# Node (si utilisé pour les scripts locaux)
node_modules/
npm-debug.log*

# Claude Code
.claude/

# Sauvegardes d'assets avant optimisation (non déployées)
assets/_originals-bak/

# Originaux non optimisés (sauvegarde locale, non déployée)
_originals-bak/

# Photos sources non utilisées par le site (gardées en local)
_unused-source/
